@model  IEnumerable<Twitter.Web.Models.ViewModels.TweetViewModel>

<div class="tweets-container">
    @if (Model.Count() == 0)
    {
        <h3>This user hasn't favorited any tweets yet.</h3>
    }
    else
    {
        @Html.DisplayForModel()
    }    
</div>
