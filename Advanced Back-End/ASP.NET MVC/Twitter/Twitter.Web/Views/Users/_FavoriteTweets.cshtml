@model  IEnumerable<Twitter.Web.Models.ViewModels.TweetViewModel>

<div class="tweets-container">
    @if (Model.Count() == 0)
    {
        <h2>This user hasn't favorited any tweets yet.</h2>
    }
    else
    {
        @Html.DisplayForModel()
    }    
</div>
